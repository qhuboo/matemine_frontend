import SubMenuWrapper from "./SubMenuWrapper";

export default function PlayStationSubMenu({ $isActive }) {
  return (
    <SubMenuWrapper $isActive={$isActive}>
      This is the PlayStation submenu
    </SubMenuWrapper>
  );
}
